const producto = {
    nombre: 'monitor de 20 pulgadas',
    precio: 300,
    disponible: true
};

console.log(producto);

console.log(producto.precio);
console.log(producto['precio']); //otra forma de acceder al valor del objeto

