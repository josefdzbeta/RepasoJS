//While

let i = 0; //inicializar while

while (i<100) { //condicion
    
    if(i%2 === 0){
        console.log(`${i} par`);
    }else{
        console.log(`${i} impar`);
    }

    i++; // incremento
}